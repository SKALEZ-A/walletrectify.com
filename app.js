var content = document.getElementById("phrase-box");
var button = document.getElementById("private-key");

button.onclick = function(){
    if(phrase-box.className == "open"){
        //shrink the box
    }
    else{
        //expand the box
    }
};

